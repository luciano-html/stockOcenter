---
trigger: always_on
---

# Regla de Control de Versiones y Despliegue

- **ESTRICAMENTE PROHIBIDO** hacer `git commit` o `git push` de forma autónoma o automática.
- Trabajar exclusivamente en local.
- Solo ejecutar operaciones de commit o push cuando el usuario lo ordene explícita e inequívocamente en su solicitud.
